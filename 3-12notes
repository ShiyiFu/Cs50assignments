printf("hello, world\n");
                                如果要输出带引号的“hello, world”，需要加转译符。

#include <stdio.h>
int main(void)
{
  printf("hello,world\n");

                                std：standard
                                io：input & output
                                .h：头文件
while(true)
{
  printf("hello, world\n");
}
                                *循环
for(int i=0;i<50;i++)
{
  printf("hello, world\n");
}
                                int：integer整数
                                i为整型变量（int代表变量类型），把右边赋值给左边
                                i++ 就是 i+1

                                C语言写完是源代码，还要转化成计算机能运行的语言才能编译运行。
if(x<y)
{
  printf("x is less than y\n");
}
else if(x>y)
{
  printf("x is more than y\n");
}
else {
  printf("x is equal to y\n");
}
                                  
                                
                                
                                
